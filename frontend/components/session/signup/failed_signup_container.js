import { connect } from 'react-redux';
import FailedSignup from './failed_signup';

const mapStateToProps = state => {
    return {
        
    };
};

const mapDispatchToProps = dispatch => {
    return {

    };
};

export default connect(mapStateToProps, null)(FailedSignup);